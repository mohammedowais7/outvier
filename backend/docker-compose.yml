version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: outvier
      POSTGRES_USER: outvier
      POSTGRES_PASSWORD: outvierpw
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
